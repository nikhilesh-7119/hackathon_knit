import 'package:flutter/material.dart';

class CostOverRunPage extends StatelessWidget {
  const CostOverRunPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}